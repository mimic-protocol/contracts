{
  "name": "@mimicprotocol/contracts",
  "private": true,
  "version": "1.0.0",
  "description": "Mimic Protocol Contracts",
  "license": "GPL-3.0",
  "scripts": {
    "prepare": "yarn build",
    "build": "yarn workspaces run build",
    "lint": "yarn workspaces run lint",
    "test": "yarn workspaces run test"
  },
  "workspaces": {
    "packages": [
      "packages/**"
    ]
  }
}
